# 2026-06-14c 변경사항 점검 체크리스트

테스트 환경: http://localhost:5173 (운영: https://dtschedule.vercel.app)

---

## 1. 50plus 기본 라벨 수정

배경: `plus_label`이 설정되지 않은 조직에서 `'50플러스활동가'`라는 특정 조직의 명칭이
전체 기본값으로 표시되던 문제. → `'50+'`로 변경.

- [ ] 조직 설정에서 `plus_label`을 설정하지 않은 상태에서:
  - SlotEditModal 배정 목록 뱃지: `'50+'` 표시
  - 월달력 plus 열 타입 버튼: `'50+'` 표시
- [ ] 조직 설정에서 `plus_label`을 커스텀 설정한 경우: 해당 값이 그대로 표시됨

---

## 2. 주간달력 split 모드 셀 배경색 통일

배경: split 모드(역할분할)에서 역할마다 서로 다른 색상(노랑/주황/보라)이 순환되어
시각적으로 혼란스러웠음. → 시간대 기준(낮=노랑, 밤=보라)으로 통일.

- [ ] split 모드 주간달력에서 같은 시간대의 역할 셀들이 모두 같은 배경색
  - 낮 슬롯(~19시): 모든 역할 노랑(`tint-sun`)
  - 밤 슬롯(20시~): 모든 역할 보라(`tint-moon`)
- [ ] 비split 모드 색상에 변화 없음

---

## 3. 기존 조직 가입 흐름 — 조직 선택 화면 추가

배경: "기존 조직에 가입하기" 선택 시 조직 선택 화면 없이 바로 가입 처리되던 문제.

- [ ] 회원가입 wizard에서 "기존 조직에 가입하기" → "계속하기" 클릭 시
  - 조직 검색 화면(`org-select` 스텝) 표시
  - 활성 테넌트가 있는 조직 목록 로드됨
- [ ] 검색 필터링 정상 동작
- [ ] 조직 선택 후 "가입 신청하기" 클릭 → 선택 조직의 `tenant_id`로 가입
- [ ] 조직 미선택 상태에서 "가입 신청하기" 비활성화

---

## 4. 회귀 테스트

- [ ] split 모드 주간달력 기존 배정 표시 정상
- [ ] 비split 모드 주간달력 50plus 살구색 유지
- [ ] SlotEditModal 커스텀 라벨 조직에서 기존과 동일하게 표시
