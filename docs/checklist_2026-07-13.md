# 2026-07-13 변경사항 점검 체크리스트

테스트 환경: http://localhost:5173 (운영: https://<운영-도메인>)
테스트 계정: 조직 관리자 권한 계정

---

## 1. 회원가입 시 전화번호 저장 (트리거 버그 수정)

배경: `061_profile_phone_on_signup.sql`이 `handle_new_user()` 트리거에서 `profiles.phone`에 값을 넣도록 변경했으나, `profiles` 테이블에 `phone` 컬럼이 없어 신규 회원가입 시 Auth API가 500 에러를 반환하던 문제를 `062_profiles_add_phone.sql`로 수정.

- [ ] 이메일 회원가입 시 전화번호 입력 후 정상 가입 완료 (500 에러 없음)
- [ ] 전화번호 없이 가입해도 정상 완료
- [ ] 카카오 등 소셜 가입 후 `vs_pending_phone` 세션스토리지 경유 전화번호 후처리 정상 동작
- [ ] **[DB 확인]** 신규 가입 계정의 `profiles.phone`에 값이 정상 저장됨

---

## 2. 관리자콘솔 회원관리 — 전화번호 표시

배경: 관리자가 회원관리 탭에서 회원 전화번호를 확인할 수 있도록 컬럼 추가.

- [ ] 데스크톱: 회원관리 테이블에 이메일 옆 "전화번호" 컬럼이 표시됨
- [ ] 전화번호 미등록 회원은 `-`로 표시됨
- [ ] 모바일 폭(≤ sm breakpoint)에서는 별도 컬럼 대신 이름 아래 작은 글씨로 전화번호가 표시됨
- [ ] 모바일에서 전화번호 텍스트를 탭하면 `tel:` 링크로 전화 연결 시도됨 (실제 발신 여부는 기기 다이얼러에 위임)
- [ ] 전화번호를 탭해도 이름 수정 등 다른 행 클릭 동작과 충돌하지 않음

---

## 3. 회귀 테스트 (기존 기능 영향 확인)

- [ ] 회원관리 탭의 이름 수정 / 역할 변경 / 권한 변경 / 삭제 기능이 기존과 동일하게 동작
- [ ] 회원 추가(이메일 초대) / 직접 등록 플로우 정상 동작
- [ ] 로그인/로그아웃, 기존 계정 재로그인 시 전화번호 관련 에러 없음

---

## 4. DB / 마이그레이션 반영 상태

- [ ] 개발 DB(`mcuszdvophmqrwostcah`) 마이그레이션 061, 062 적용 확인 완료
- [ ] **[운영 DB]** `npx supabase migration list --linked` 결과 061, 062 Local/Remote 일치 (운영 반영 완료)
- [ ] `supabase/reset_db.sql`이 062 기준 최신 스키마(phone 컬럼 포함)로 갱신됨
- [ ] `npx tsc -b` 통과 확인

---

## 5. 다크모드 `<select>` 드롭다운 가시성 수정

배경: 다크모드에서 관리자콘솔 조직 선택 등 `<select>` 드롭다운을 열면 팝업(옵션 목록) 배경이 브라우저 기본값(흰색)으로 렌더링되어 밝은 글자색과 겹쳐 텍스트가 안 보이던 문제. `color-scheme` CSS만으로는 일부 브라우저에서 select 팝업에 반영되지 않아, `.dark select`/`.dark select option`에 배경·글자색을 직접 지정하는 방식으로 수정.

- [ ] 다크모드에서 관리자콘솔 조직 선택 드롭다운을 열었을 때 옵션 목록 배경이 어둡게 표시되고 조직명이 읽힘
- [ ] 다크모드에서 회원관리 탭의 역할/권한 select, 날짜 규칙 탭의 색상/시간 select 등 다른 select들도 옵션 목록이 정상적으로 보임
- [ ] 라이트모드에서는 기존과 동일하게 흰 배경에 어두운 글자로 표시됨 (회귀 없음)
- [ ] 스케줄 페이지 등 관리자콘솔 외 다른 화면의 select도 다크모드에서 정상 표시됨

---

## 6. D-1 배정알림 cron 30분 단위 변경

배경: 관리자콘솔에서 발송 시간을 30분 단위(예: 18:30)로 설정할 수 있는데, GitHub Actions cron이 매시 정각에만 실행되어 `:30`으로 설정한 조직은 알림이 영영 발송되지 않던 문제를 수정. cron을 `*/30 * * * *`로 변경.

- [ ] 발송 시간을 `:30`으로 설정한 조직에서 해당 시각에 자동 발송이 정상 동작함
- [ ] 기존처럼 정시(`:00`)로 설정한 조직도 계속 정상 동작함
- [ ] GitHub Actions "Send D-1 Reminders" 워크플로우 실행 기록이 30분 간격으로 쌓임 (Actions 탭에서 확인)

---

## 7. 배정알림 발송내역 조회

배경: 관리자가 조직 단위로 최근 발송된 D-1 알림(인앱/웹푸시)을 조회할 수 있는 기능 추가. 기존 `notifications` RLS는 본인 것만 조회 가능해 관리자가 다른 회원의 발송 기록을 볼 수 없었음 → 조직 관리자/슈퍼관리자용 SELECT 정책 추가.

- [ ] 관리자콘솔 → 배정알림 탭 하단 "발송내역" 섹션에서 최근 발송 목록이 보임 (수신자·메시지·발송일시·읽음여부)
- [ ] "지금 발송" 클릭 후 발송내역이 자동으로 갱신되어 새 항목이 나타남
- [ ] "새로고침" 버튼으로 수동 갱신 가능
- [ ] 다른 조직 관리자로 전환 시 발송내역도 해당 조직 것으로 바뀜 (조직 간 데이터 섞이지 않음)
- [ ] **[DB 확인]** `notif_select_tenant_admin`, `notif_select_superadmin` 정책이 개발 DB에 적용됨 (`npx supabase migration list --linked`)
- [ ] **[운영 DB]** 063 마이그레이션 운영 반영 후에도 동일하게 동작

---

## 테스트 우선순위

1. **1번** 회원가입 500 에러 재현 여부 — 운영 반영된 핵심 버그 수정이므로 최우선 확인
2. **6번** D-1 알림 30분 단위 cron — 실제 자동 발송이 안 되던 버그이므로 우선 확인
3. **7번** 배정알림 발송내역 조회 — 신규 RLS 정책 포함이므로 권한 회귀 여부 함께 확인
4. **5번** 다크모드 select 가시성 — 여러 화면에 공통 적용되는 CSS 변경이므로 다크/라이트 모두 확인
5. **2번** 관리자콘솔 전화번호 표시/tel 링크 — 신규 기능이므로 데스크톱·모바일 모두 확인
6. **3번** 회귀 테스트 — 회원관리 탭 기존 기능 영향 없는지 확인
