# 스케줄 이미지 첨부 기능 점검 체크리스트 (2026-06-25)

> **사전 조건:** Supabase SQL Editor에서 `056_storage_schedule_images.sql` 마이그레이션 실행 후 점검

## 1. 커스텀 필드 설정 (Step7)

- [ ] 테넌트 설정 → 스텝 7 (커스텀 필드) → 새 필드 추가 → 타입 드롭다운에 **"이미지첨부"** 항목이 보이는가?
- [ ] 이미지첨부 필드 추가 후 저장 — 오류 없이 저장되는가?
- [ ] 필드 카드에 📷 아이콘과 "이미지 첨부 (최대 3장, WebP 자동 압축)" 미리보기가 보이는가?

## 2. 스케줄 등록 팝업 — 이미지 첨부 UI

- [ ] 스케줄 등록 팝업 열기 → 이미지첨부 필드 "📷 사진 추가 (3장 가능)" 버튼이 보이는가?
- [ ] JPG/PNG 이미지 선택 → "⏳ 이미지 최적화 중..." 로딩 텍스트가 표시되는가?
- [ ] 압축 완료 후 썸네일과 압축 후 용량(예: "87KB")이 표시되는가?
- [ ] 썸네일에 마우스 올리면 X 버튼이 나타나고 클릭 시 해당 이미지만 제거되는가?
- [ ] 3장 선택 후 "사진 추가" 버튼이 사라지는가?

## 3. 원본 크기 가드

- [ ] 20MB 초과 파일 선택 시 즉시 오류 메시지가 표시되고 선택이 차단되는가?

## 4. 스케줄 등록 (DB 저장)

- [ ] 이미지 첨부 후 "저장" 버튼 클릭 → Storage 업로드 후 정상 등록되는가?
- [ ] Supabase Dashboard → Storage → schedule-images → `{tenantId}/` 폴더에 .webp 파일이 업로드됐는가?
- [ ] 등록된 어사인먼트 카드에 **"🖼 이미지 N장"** 뱃지가 표시되는가?

## 5. 이미지 뷰어

- [ ] "🖼 이미지 N장" 뱃지 클릭 → 전체화면 라이트박스가 열리는가?
- [ ] 이미지 N장 중 "N / M" 카운터가 표시되는가?
- [ ] "← 이전" / "다음 →" 버튼으로 이미지 이동이 되는가?
- [ ] 키보드 ← → 화살표로 이동, ESC로 닫기가 되는가?
- [ ] 배경(backdrop) 클릭으로 닫기가 되는가?

## 6. 수정 모드

- [ ] 기존 이미지가 있는 어사인먼트 수정 → 기존 이미지 썸네일이 표시되는가?
- [ ] 기존 이미지 X 버튼으로 개별 삭제 후 저장 → 해당 이미지가 목록에서 제거되는가?
- [ ] 수정 시 새 이미지 추가 후 저장 → 기존 이미지 + 신규 이미지 모두 표시되는가?

## 7. 알려진 제한 사항 (향후 개선)

- 어사인먼트 삭제 시 Storage 파일은 자동 삭제되지 않음 (Storage → Objects에서 수동 삭제 필요)
- 이미지 제거(X 클릭) 후 저장 시에도 Storage의 원본 파일은 남음

## Supabase 수동 설정 (미완료 시)

- [ ] SQL Editor → `supabase/migrations/056_storage_schedule_images.sql` 실행
- [ ] Storage → Buckets → `schedule-images` 버킷 확인 (public: ON)
- [ ] Storage → Policies → `schedule-images`에 3개 정책 확인
