# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-promo
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Local npm config (Windows PowerShell shell fix — not for Vercel/Linux)
.npmrc

# Environment variables (never commit secrets)
.env
.env.*
!.env.example
# Vertical brand env files (no secrets — safe to commit)
!.env.lesson-on
!.env.shift-on
!.env.serve-on
!.env.class-on
!.env.work-on
!.env.salon-on
!.env.care-on

# Generated design tokens (built from tokens.json)
src/styles/tokens.css
.env*
!.env.lesson-on
!.env.shift-on
!.env.serve-on
!.env.class-on
!.env.work-on
!.env.salon-on
!.env.care-on

# Android 서명 키스토어 (절대 커밋 금지)
android/app/keystore.properties
android/app/keystores/

# @capacitor/assets 소스 이미지 (빌드 산출물)
assets/icon-only.png
assets/icon-background.png
assets/splash.png
